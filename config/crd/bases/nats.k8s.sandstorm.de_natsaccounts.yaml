---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: natsaccounts.nats.k8s.sandstorm.de
spec:
  group: nats.k8s.sandstorm.de
  names:
    kind: NatsAccount
    listKind: NatsAccountList
    plural: natsaccounts
    singular: natsaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.userCount
      name: Users
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NatsAccount is the Schema for the natsaccounts API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NatsAccountSpec defines the desired state of NatsAccount.
            properties:
              clusterRef:
                description: |-
                  ClusterRef references the NatsCluster this account belongs to.
                  Must be in the same namespace as this NatsAccount.
                properties:
                  name:
                    description: Name of the referenced resource.
                    type: string
                required:
                - name
                type: object
              jetstream:
                description: JetStream configures JetStream resource limits for this
                  account.
                properties:
                  diskMaxStreamBytes:
                    anyOf:
                    - type: integer
                    - type: string
                    description: DiskMaxStreamBytes is the per-stream disk storage
                      ceiling.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxAckPending:
                    description: MaxAckPending is the maximum number of pending ACKs
                      per consumer.
                    format: int64
                    type: integer
                  maxBytesRequired:
                    description: MaxBytesRequired requires all streams to have max_bytes
                      set when true.
                    type: boolean
                  maxConsumers:
                    description: MaxConsumers is the maximum number of consumers allowed.
                    format: int64
                    type: integer
                  maxFile:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MaxFile is the maximum file/disk-based storage for
                      this account (e.g. "1Gi", "10Gi").
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxMemory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MaxMemory is the maximum in-memory storage for this
                      account (e.g. "512Mi", "1Gi").
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxStreams:
                    description: MaxStreams is the maximum number of streams allowed.
                    format: int64
                    type: integer
                  memoryMaxStreamBytes:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MemoryMaxStreamBytes is the per-stream memory storage
                      ceiling.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              limits:
                description: Limits configures connection and resource limits for
                  this account.
                properties:
                  maxConnections:
                    description: MaxConnections is the maximum number of concurrent
                      client connections.
                    format: int64
                    type: integer
                  maxLeafnodes:
                    description: MaxLeafnodes is the maximum number of leaf node connections.
                    format: int64
                    type: integer
                  maxPayload:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MaxPayload is the maximum message payload size (e.g.
                      "1Mi", "64Mi").
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxSubscriptions:
                    description: MaxSubscriptions is the maximum number of subscriptions
                      per connection.
                    format: int64
                    type: integer
                type: object
              userRules:
                description: |-
                  UserRules is an ordered list of rules controlling which NatsUsers can reference
                  this account. Rules are evaluated in order; the first matching rule's action applies.
                  If no rule matches, access is denied.
                items:
                  description: |-
                    UserRule is a single ordered rule evaluated against a NatsUser's namespace.
                    Exactly one of NamespaceRegex, NamespaceLabels, or SameNamespace must be set.
                  properties:
                    action:
                      description: Action is grant or deny.
                      enum:
                      - grant
                      - deny
                      type: string
                    namespaceLabels:
                      additionalProperties:
                        type: string
                      description: |-
                        NamespaceLabels matches namespaces whose labels contain all key/value pairs
                        in this map (equality matching).
                      type: object
                    namespaceRegex:
                      description: NamespaceRegex is a Go regular expression matched
                        against the user's namespace name.
                      type: string
                    sameNamespace:
                      description: SameNamespace, when true, matches users that are
                        in the same namespace as this NatsAccount.
                      type: boolean
                  required:
                  - action
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one of namespaceRegex, namespaceLabels, or sameNamespace
                      must be set
                    rule: (has(self.namespaceRegex) && !has(self.namespaceLabels)
                      && !has(self.sameNamespace)) || (!has(self.namespaceRegex) &&
                      has(self.namespaceLabels) && !has(self.sameNamespace)) || (!has(self.namespaceRegex)
                      && !has(self.namespaceLabels) && has(self.sameNamespace))
                type: array
            required:
            - clusterRef
            type: object
          status:
            description: NatsAccountStatus defines the observed state of NatsAccount.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the account's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              userCount:
                description: UserCount is the number of NatsUsers in this account.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
